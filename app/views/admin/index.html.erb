<% if @members.any? %>
  <% @members.each do |member| %>
    <h2> <%= member.name %> </h2>
  <% end %>
<% else %>
  No members have been added yet.
<% end %>

<a href='/members/new'>Add a new member</a>
