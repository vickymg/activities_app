<%= form_for [@member, @activity] do |f| %>
  <%= f.label :description %>
  <%= f.collection_select :description, @activities, :description, :description %>
  <%= f.submit 'Add activity' %>
<% end %>
